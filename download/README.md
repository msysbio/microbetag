1. For local run:

    - `edgelist.csv` 
    - `thirty_Samples.tsv`
    - `config.yml`

2. For running `microbetag` through the MGG app with a network already available:
    - `vitAbund.tsv`
    - `edgelist.tsv`

and 
    - `testAbund.tsv`, if no co-occurrence network is provided 

3. For the `microbetag` preprocess steps:

    - `config_prep.yml`
    - `seq_ab_tab.tsv`

4. For the loading nets tutorial: 
    - `network_output.edgelist`
    - `microbetag_annotated.cx`





